# libra

사람과 여러 AI가 함께 대화하는 채팅방.
초점은 대화의 사회성과 비용 안전성.

## 지금 어디까지 왔나

설계는 반영 완료했고, 구현은 시작 단계다.

- 설계 명세: `docs/superpowers/specs/2026-09-14-libra-design.md` ← **확인 필수**
- 세션 이관 메모: `docs/HANDOFF.md`
- 다음 단계: `CLI 동작과 최소 대화 검증을 포함한 구현 계획 작성`

## 설계 확정 값

1. 독립 응답 3자 동시 동작을 기본으로 한다.
   누군가 답한다 해서 다른 답을 자동 취소하지 않는다.
   새 메시지 도착 시 계속/고쳐 쓰기/중단은 참여자 판단(또는 정책)에 따라 결정한다.
2. 침묵은 모델 호출을 의미하지 않는다.
   사람이 말을 안 하더라도 비용이 발생하지 않는다.
   오직 별도 깨우기 이벤트(타이머/조건)에서만 새 호출이 일어날 수 있다.
3. 사용량은 조회되지 않으면 `미확인`으로 표시한다.
   조회 실패를 0으로 바꾸지 않는다.
   제한 초과 의심 시 해당 참가자를 쉬게 두고 리셋/확인 후 재개한다.
4. 게스트 제한은 주인 측에서 강제된다. 토큰/비용 상한은 우회하지 않는다.

## 핵심 스택

- Headless JSON 스트림(PTY 아님)
- Ink TUI 우선
- Telegram 브릿지(향후)
- TypeScript + Node
- 저장은 JSONL 로그
- 초기 참여자: Claude, Codex

## 개발 규칙

- 실행과 실험은 가능한 최저가 모델로:
  - claude: `haiku` 계열
  - codex: `luna` 계열
  - 정확한 모델 식별자는 실제 실행 전 검증
- 프로젝트 이름은 `libra` 고정, 방 이름·실행명은 설정으로 분리
- 어댑터 인터페이스는 로컬/원격 동형이며 Core는 클라이언트 종류를 알지 않는다.

## 미검증 항목 (구현 착수 순서)

1. CLI 생성 중단/재개/새 입력 처리 동작 검증
2. 사용량 조회 실패 시 동작, 리밋 초과 대응 정책
3. 정확한 모델 식별자 및 provider 비용 이벤트
4. 실제 대화 품질(중복 말투, 과잉 동의, 과도 침묵)
5. 게스트 도구 제한/상한 강제 적용
