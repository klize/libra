# chatai

사람 한 명과 여러 AI 에이전트(claude, codex, gemini, cursor 등)가 함께 대화하는 채팅방.
핵심은 **대화의 사회성**과 **비용 안전성** 두 가지다.

## 지금 어디까지 왔나

설계 브레인스토밍 완료. 구현은 시작 전.

- 설계 명세: `docs/superpowers/specs/2026-09-14-chatai-design.md` ← **먼저 읽을 것**
- 세션 이관 메모: `docs/HANDOFF.md`
- 다음 단계: 구현 계획 작성 (superpowers:writing-plans)

## 설계의 핵심 세 가지

1. **의향 타진 후 발언.** 넷이 동시에 답을 생성하면 셋은 버려진다.
   먼저 "끼어들 말이 있나"를 한 줄로 싸게 묻고, 뽑힌 하나만 전체 답변을 만든다.
   의향 없음이 곧 자연스러운 침묵이 된다.

2. **침묵은 입력이다.** 아무도 말하지 않는 상태 자체를 이벤트로 방송해서
   AI가 스스로 말문을 열게 한다. 그대로 두면 토큰이 증발하므로 자동 휴면 타이머가 짝을 이룬다.

3. **방어선은 소유자 쪽에.** 타인 구독으로 참여한 에이전트는 그 사람 PC의 데몬이 상한을 들고 있고
   방 서버의 요청을 거절할 수 있다. 서버를 신뢰하지 않아도 안전하다.

## 확정 사항

| 항목 | 값 |
|---|---|
| 연결 방식 | Headless JSON 스트림 (PTY 아님) |
| 1급 UI | 터미널 TUI (Ink). 웹은 얇은 뷰 |
| 모바일 | 텔레그램 봇 브리지 |
| 스택 | TypeScript + Node |
| 저장 | JSONL 이벤트 로그. DB 없음 |
| 초기 참여자 | claude, codex |

## 개발 규칙

- **테스트는 반드시 최저가 모델로.** claude는 haiku, codex는 luna 계열.
  실제 대화 품질 확인이 목적이 아닌 모든 실행에 적용한다.
- 프로젝트 이름 `chatai`는 잠정이다. 코드와 설정에 하드코딩하지 않는다.
- 어댑터는 로컬과 원격이 같은 인터페이스를 낸다. Room Core는 클라이언트 종류를 모른다.

## 미검증 사항 (구현 착수 시 최우선 확인)

1. 두 CLI의 토큰 사용량 이벤트 필드명
2. codex 저가 모델(luna)의 정확한 식별자
3. 생성 중단 시 과금 범위
4. `claude --max-budget-usd` 초과 신호의 형태
